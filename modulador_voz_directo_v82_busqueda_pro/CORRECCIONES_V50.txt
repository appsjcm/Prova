CORRECCIONES V50 (revisión de código)

Errores de arranque (la app se cerraba antes de abrir la ventana):
- Inicializados atributos que faltaban en el arranque: favoritos, perfiles,
  sonidos personalizados, índice del Cambiador, tema actual, preset del
  Ecualizador y campos del Creador de voces.
- Favoritos y perfiles ahora se cargan ANTES de construir la interfaz,
  para que sus pestañas muestren los datos guardados.
- Corregido el botón "Actualizar dispositivos" de Cable Virtual, que
  llamaba a un método inexistente.
- Corregida la ventana de bienvenida, que nunca llegaba a mostrarse.

Exportaciones que fallaban en silencio:
- Corregido el error datetime.datetime.now() -> datetime.now() en 11 sitios.
  Vuelven a funcionar: Exportar resumen del Studio Dashboard (JSON/TXT),
  Exportar Master WAV, guardar proyecto del Mezclador, exportar
  instrumental y letra de Canción Pro, informe del Analizador Vocal,
  exportar Cadena Vocal y exportar Timeline (JSON y TXT).

Instalador Windows:
- Corregido instalador/ModuladorVozDirectoV50.iss: comillas sin cerrar en
  4 líneas y accesos directos sin el parámetro Filename. Ahora compila
  con Inno Setup 6.

Limpieza:
- Eliminada la carpeta src/__pycache__ del paquete (13 MB de archivos
  compilados de versiones anteriores que no deben distribuirse).
